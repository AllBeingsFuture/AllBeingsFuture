# Skills directory (empty by default)

This app does **not** ship a built-in skill catalog.

Users add skills through the app (Install / custom add). Leave this directory empty unless you are developing a custom packaging layout.

Nothing under this path is auto-discovered or seeded into the skill registry on startup.

name: Build Pre-Release

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: pre-release-publish
  cancel-in-progress: false

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            package-lock.json
            frontend/package-lock.json

      - name: Install Dependencies
        run: npm ci

      - name: Install Frontend Dependencies
        working-directory: frontend
        run: npm ci

      - name: Setup Python (for node-gyp)
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Rebuild Native Modules for Electron
        run: npx @electron/rebuild --force --only better-sqlite3

      - name: Compute Release Metadata
        id: meta
        if: github.event_name != 'pull_request'
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          $pkg = Get-Content package.json | ConvertFrom-Json
          $stableVersion = [string]$pkg.version
          $parts = $stableVersion.Split('.')
          if ($parts.Length -lt 3) {
            Write-Error "Unsupported package version: $stableVersion"
            exit 1
          }

          $major = [int]$parts[0]
          $minor = [int]$parts[1]
          $basePatch = [int]($parts[2] -replace '[^0-9].*$', '')

          $headers = @{
            Authorization = "Bearer $env:GITHUB_TOKEN"
            Accept        = "application/vnd.github+json"
            "User-Agent"  = "allbeingsfuture-prerelease-workflow"
          }
          $releases = Invoke-RestMethod -Headers $headers -Uri "https://api.github.com/repos/$env:GITHUB_REPOSITORY/releases?per_page=100"
          $pattern = "^betaV$major\.$minor\.(\d+)$"
          $maxPatch = $null

          foreach ($release in $releases) {
            $tagName = [string]$release.tag_name
            if ($tagName -match $pattern) {
              $patch = [int]$Matches[1]
              if ($null -eq $maxPatch -or $patch -gt $maxPatch) {
                $maxPatch = $patch
              }
            }
          }

          if ($null -eq $maxPatch) {
            $nextPatch = $basePatch
          } else {
            $nextPatch = $maxPatch + 1
          }

          $displayVersion = "betaV$major.$minor.$nextPatch"
          $packageVersion = "$major.$minor.$nextPatch-beta.0"
          $tag = $displayVersion
          $name = $displayVersion
          $artifact = "release-$displayVersion"

          "tag=$tag" >> $env:GITHUB_OUTPUT
          "name=$name" >> $env:GITHUB_OUTPUT
          "artifact=$artifact" >> $env:GITHUB_OUTPUT
          "display_version=$displayVersion" >> $env:GITHUB_OUTPUT
          "package_version=$packageVersion" >> $env:GITHUB_OUTPUT

      - name: Build Windows Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
          ABF_DISPLAY_VERSION: ${{ steps.meta.outputs.display_version }}
          ABF_PACKAGE_VERSION: ${{ steps.meta.outputs.package_version }}
        run: |
          if ($env:ABF_PACKAGE_VERSION) {
            npm run build
            npx electron-builder --win "-c.extraMetadata.version=$env:ABF_PACKAGE_VERSION" "-c.win.artifactName=AllBeingsFuture Setup $env:ABF_DISPLAY_VERSION.`${ext}"
          } else {
            npm run build
            npx electron-builder --win
          }

      - name: Locate Installer Asset
        id: installer
        shell: pwsh
        run: |
          $installer = Get-ChildItem release -File -Filter '*Setup*.exe' | Sort-Object LastWriteTime -Descending | Select-Object -First 1
          if (-not $installer) {
            Write-Error 'No setup installer was produced under release/.'
            exit 1
          }
          "path=$($installer.FullName)" >> $env:GITHUB_OUTPUT
          "name=$($installer.Name)" >> $env:GITHUB_OUTPUT

      - name: Upload Workflow Artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.meta.outputs.artifact }}
          path: ${{ steps.installer.outputs.path }}
          if-no-files-found: error

      - name: Publish GitHub Pre-Release
        if: github.event_name != 'pull_request'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.meta.outputs.tag }}
          name: ${{ steps.meta.outputs.name }}
          target_commitish: ${{ github.sha }}
          prerelease: true
          generate_release_notes: true
          files: ${{ steps.installer.outputs.path }}
